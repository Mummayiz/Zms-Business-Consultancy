# Source files: normalise to LF in the repo and working tree
* text=auto eol=lf

# Brand originals and binaries: store byte-for-byte, never convert
brand-assets/** -text
*.pdf binary
*.png binary
*.jpg binary
*.ico binary
